void floorcast(Player player) {


	/*
	Sprite sprite = textured_walls[1];
	SET_COLOR(renderer_3D, 210, 180, 140);

	for (int y = half_screen_height; y < screen_height; y++) {
		for (double x = 0; x < screen_width; x += step_x) {
			double dist_wall = z_buffer[(int) x];
			SDL_RenderDrawPoint(renderer_3D, x, y + player.pace.height);
		}
	}
	*/
}
